{
  "name": "art-mcp-permission-manager",
  "version": "0.2.1",
  "license": "MIT",
  "type": "module",
  "description": "ART MCP Permission Manager core SDK (fork of CORS Unblock). Provides install helpers and website↔extension messaging for managed CORS permissions.",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./internal": "./src/internal.ts"
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "init": "pnpm build",
    "build": "vite build",
    "dev": "pnpm build --watch",
    "test": "vitest run"
  },
  "sideEffects": false,
  "devDependencies": {
    "@liuli-util/vite-plugin-node": "^0.10.0",
    "typescript": "^5.8.3",
    "vite": "^6.2.5",
    "vitest": "^3.1.1"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "dependencies": {
    "@webext-core/messaging": "^2.2.0"
  }
}
